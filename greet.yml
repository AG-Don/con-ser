msg: ok, we have to find it immadately it down


server:
  port: 8083

debug: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


management:
  endpoints:
    web:
      exposure:
        include: refresh
